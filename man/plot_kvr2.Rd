% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_kvr2.R
\name{plot_kvr2}
\alias{plot_kvr2}
\title{Plot Method for Kvalseth's R-squared Objects}
\usage{
plot_kvr2(
  x,
  type = c("auto", "linear", "power"),
  plot_type = c("both", "r2", "diag"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{lm}.}

\item{type}{Character string. Selects the model type: \code{"linear"}, \code{"power"},
or \code{"auto"} (default). In \code{"auto"}, the function detects if the dependent
variable is log-transformed.}

\item{plot_type}{A string specifying the plot layout: \code{"both"} (default) displays
the bar plot and diagnostic plot side-by-side using \code{par(mfrow = c(1, 2))},
\code{"r2"} shows only the R-squared comparison,
and \code{"diag"} shows only the observed-vs-predicted plot.}

\item{...}{Further graphical parameters passed to \code{barplot()} or \code{plot()}.}

\item{r2}{type}
}
\value{
The function is called for its side effect of generating a plot.
It returns \code{x} invisibly.
}
\description{
Visualizes the different R-squared definitions or provides a diagnostic
observed-vs-predicted plot to understand the model fit.
}
\details{
When \code{plot_type = "r2"}, the function creates a bar plot comparing all nine
definitions. Bars are colored based on their validity:
\itemize{
\item \strong{Skyblue}: Standard values between 0 and 1.
\item \strong{Orange}: Values exceeding 1.0 or falling below 0.0 (warnings).
}

When \code{plot_ype = "diag"}, the function displays a scatter plot of observed
vs. predicted values. Two reference lines are added:
\itemize{
\item \strong{Darkgreen Solid Line}: The 1:1 "perfect fit" line (RSS reference).
\item \strong{Red Dashed Line}: The overall mean of the observed data (TSS reference).
}
If the data points are closer to the red dashed line than the green solid line,
\eqn{R^2_1} will be negative.

\strong{Combined View (\code{plot_type = "both"})}:
Automatically configures the plotting device to show both plots simultaneously
for a comprehensive model evaluation.
}
\examples{
df1 <- data.frame(x = 1:6, y = c(15, 37, 52, 59, 83, 92))
model <- lm(y ~ x - 1, data = df1) # No-intercept model
plot_kvr2(model)
# Compare all definitions
plot_kvr2(model, plot_type = "r2")

# Diagnostic plot to see why some R2 might be problematic
plot_kvr2(model, plot_type = "diag")

}
