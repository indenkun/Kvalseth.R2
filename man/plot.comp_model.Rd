% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_comp_model.R
\name{plot.comp_model}
\alias{plot.comp_model}
\title{Plot Comparison of Model Specifications}
\usage{
\method{plot}{comp_model}(x, ...)
}
\arguments{
\item{x}{An object of class \code{comp_model} generated by \code{\link[=comp_model]{comp_model()}}.}

\item{...}{Further graphical parameters (currently ignored).}
}
\value{
This function is primarily called for its side effect of creating a
\code{grid}-based plot. It returns the input object \code{x} invisibly.
}
\description{
Generates a comprehensive 2x2 diagnostic dashboard comparing models with and
without an intercept. This visualization helps identify how the absence of
an intercept affects different R-squared definitions and error metrics.
}
\details{
The plot is organized into four panels:
\itemize{
\item \strong{Top-Left}: Grouped bar chart of the nine R-squared definitions.
\item \strong{Bottom-Left}: Comparison of absolute fit metrics (RMSE, MAE, MSE).
\item \strong{Top-Right}: Observed vs. Predicted plot for the intercept model.
\item \strong{Bottom-Right}: Observed vs. Predicted plot for the no-intercept model.
}
This layout allows for a direct "cause-and-effect" analysis: for instance,
observing a data point far from the identity line in the bottom-right panel
explains why certain R-squared definitions might crash or become negative
in the left panels.
}
\note{
Since this plot uses the \code{grid} system to combine multiple \code{ggplot}
objects, it cannot be further modified with the \code{+} operator. If you
need to customize individual panels, use the internal plotting functions
or extract the models from the \code{comp_model} object attributes.
}
\examples{
\dontrun{
df <- data.frame(x = 1:5, y = c(2, 3, 5, 4, 6))
m1 <- lm(y ~ x)
res <- comp_model(m1)
plot(res)
}

}
\seealso{
\code{\link[=comp_model]{comp_model()}}, \code{\link[=plot_diagnostic]{plot_diagnostic()}}
}
